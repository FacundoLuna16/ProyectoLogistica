# Configuración de conexión a la base de datos

# Configuración de MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/swiftlogix
spring.datasource.username=dev
spring.datasource.password=andes2020
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuración de Hibernate
spring.jpa.hibernate.ddl-auto=update

#logging.level.org.hibernate.SQL=DEBUG

# Keycloak configuration
spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:8180/auth/realms/siglo21
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=http://localhost:8180/auth/realms/siglo21/protocol/openid-connect/certs

jwt.auth.converter.resource-id=siglo21-client-api-rest
jwt.auth.converter.principle-attribute=preferred_username

#Optional for debugging
logging.level.org.keycloak=DEBUG


